<div class="row " style="margin-top: 50px">
    <div class="col-md-6 col-md-offset-3 form-container">
        <h2>Review</h2>
        <p>
            Please provide your review below:
        </p>

        <div class="row mt-5">
            <div class="col-5 searchResults">
                <div class="row" data-imgURl="{{data.imgURL}}" data-track_name="{{data.track_name}}" data-track_artist="{{data.track_artist}}" data-track_id="{{data.track_id}}">
                    <div class="col-3 searchresultimg">
                        <img class="resultImage" src="{{data.imgURL}}" alt="Result Image Here" style="width:100px;height:100px;">
                    </div>
                    <div class="col-9  text-white">
                        <p>{{data.track_name}}</p>
                        <p>{{data.track_artist}}</p>

                    </div>
                    <a id="chooseReviewItemButton" class="btn btn-primary">Review</a>
                </div>       
            </div>
        </div>


        <div class="input-group">
            <textarea class="form-control" placeholder="Enter your review here..." aria-label="With textarea"></textarea>
            <button type="submit" class="btn btn-lg btn-success btn-block">Write Review</button>
        </div>
    </div>
</div>
    

