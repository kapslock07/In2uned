##FRONT_END


NEED:

$.get "/api/feed" => gets feed page with reviews

$.post "/api/feed" => sends all required data to server to store review


##BACK_END

NEED: 

GET "/api/feed" => Makes a call to the db and returns all user reviews, also gets data from Spotify api such as an image and playback, adds to res object and sends to client

POST "/api/feed" => Adds a review to the db


WANT:

GET "/api/feed/:contentType" => returns data that matches content type. either: Song, Album, or Artist

PUT "/api/feed" => so users can update reviews
